# product of sum of even place digits and sum of odd place digits

rate = list(map(int,input("Enter rate :: ")))
even = []
odd = []
for i in rate:
    if i%2==0:
        even.append(i)
    else:
        odd.append(i)
print(sum(even)*sum(odd))

output :
==========

Enter rate :: 69432
144
